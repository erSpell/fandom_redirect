{
  "name": "fandom-to-antifandom-redirector",
  "version": "0.1.0",
  "private": true,
  "description": "Firefox WebExtension called Fandom to Antifandom that redirects Fandom wiki pages to matching Antifandom wiki pages.",
  "scripts": {
    "test": "node test_redirect.js",
    "validate:json": "python -m json.tool manifest.json > /dev/null",
    "build": "python scripts/build_zip.py",
    "build:zip": "python scripts/build_zip.py"
  },
  "devDependencies": {}
}
